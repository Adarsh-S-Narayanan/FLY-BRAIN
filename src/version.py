"""FlyBrain version metadata (single source of truth for V4)."""
VERSION = "4.0.0"
VERSION_TAG = "v4.0.0"
RELEASE_NAME = "FlyBrain V4.0.0"


def version_string() -> str:
    return f"FlyBrain {VERSION}"
